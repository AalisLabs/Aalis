# @aalis/plugin-subtask

子任务能力。注册 `subtask` 工具组（create_subtask / check_subtask / send_to_subtask / delete_subtask / wait_subtasks），并通过 agent 中间件实现父/子会话的上下文注入与自动完成。

详见 [docs/plugins/plugin-subtask.md](../../docs/plugins/plugin-subtask.md)。

## 安装

```bash
pnpm add @aalis/plugin-subtask
```
