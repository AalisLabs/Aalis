# @aalis/api-llm

导出服务描述符（运行时值）与类型；不含服务实现

## 角色

导出服务描述符（运行时值）与类型；不含服务实现

## 安装

```bash
pnpm add @aalis/api-llm
```

## 使用

```ts
import { llm } from '@aalis/api-llm';

// uses: { llm }
```

## 许可

见仓库根目录 LICENSE。
