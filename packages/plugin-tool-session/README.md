# @aalis/plugin-tool-session

会话历史读取工具。提供 `session_get_history` 工具与 `session-history` 服务，按 Aalis sessionId 读取指定会话最近若干条消息（默认仅同平台范围内）。

详见 [docs/plugins/plugin-tool-session.md](../../docs/plugins/plugin-tool-session.md)。

## 安装

```bash
pnpm add @aalis/plugin-tool-session
```
