# @aalis/plugin-memory-api

类型/接口/能力声明包（只导出 `type` 与 `*Capabilities`，不含运行时逻辑）

## 角色

类型/接口/能力声明包（只导出 `type` 与 `*Capabilities`，不含运行时逻辑）

## 安装

```bash
pnpm add @aalis/plugin-memory-api
```

## 使用

```ts
import type { /* ... */ } from '@aalis/plugin-memory-api';
```

本包为 *-api 类型包；实现见对应的运行时插件。

## 许可

见仓库根目录 LICENSE。
