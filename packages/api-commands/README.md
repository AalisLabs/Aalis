# @aalis/api-commands

导出服务描述符（运行时值）与类型；不含服务实现

## 角色

导出服务描述符（运行时值）与类型；不含服务实现

## 安装

```bash
pnpm add @aalis/api-commands
```

## 使用

```ts
import type { /* ... */ } from '@aalis/api-commands';
```

本包为 *-api 类型包；实现见对应的运行时插件。

## 许可

见仓库根目录 LICENSE。
